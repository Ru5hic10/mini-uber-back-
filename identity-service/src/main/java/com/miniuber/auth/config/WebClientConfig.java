package com.miniuber.auth.config;

import org.springframework.context.annotation.Configuration;

/**
 * WebClient configuration removed - services call each other directly.
 * Placeholder to avoid classpath issues if referenced elsewhere.
 */
@Configuration
public class WebClientConfig {

}