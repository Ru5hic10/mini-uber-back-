package com.miniuber;

// Previously used for test bootstrapping. Disabled to allow tests to use
// the main application class and test properties. Left intentionally
// empty so it won't be picked up as a @SpringBootConfiguration.
public class TestConfiguration {

}
